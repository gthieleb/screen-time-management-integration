# Replace before apply: echo -n 'strong-pw' | base64
# Or create via kubectl to avoid committing the real password:
#   kubectl create secret generic pihole-webpassword -n pihole \
#     --from-literal=PIHOLE_WEBPASSWORD='<strong-pw>' --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: pihole-webpassword
  namespace: pihole
type: Opaque
data:
  # base64 of <CHANGE_ME_STRONG_PASSWORD>
  PIHOLE_WEBPASSWORD: PENIQU5HRV9NRV9TVFJPTkdfUEFTU1dPUkQ+